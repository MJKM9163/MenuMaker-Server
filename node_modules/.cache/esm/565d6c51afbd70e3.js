let jwt;_afb‍.x([["default",()=>_afb‍.o]]);_afb‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if (!token) {
        return next();
    };
    try {
        const verify = jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id: verify._id,
            username: verify.username,
        };
        _afb‍.g.console.log("verify",verify);
        return next();
    } catch (e) {
        return next();
    };
};

_afb‍.d(jwtMiddleware);