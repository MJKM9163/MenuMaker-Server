let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_ce1‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_ce1‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_ce1‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_ce1‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_ce1‍.w("./api",[["default",["api"],function(v){api=v}]]);_ce1‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config(); // .env 파일에서 환경변수 불러오기







//rhkrrbaud:apsbfh9163@3.144.241.82:27017
const URI = `mongodb://localhost:27017/menumaker`
const { PORT } = process.env;

mongoose
.connect(URI, { useUnifiedTopology: true, useNewUrlParser: true })
    .then(() => {
        console.log('몽고DB 연결');
    })
    .catch(e => {
        _ce1‍.g.console.error(e);
        console.log('연결 실패!');
    });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    console.log('port 4000 연결');
});